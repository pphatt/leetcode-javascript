/**
 * @param {string} s
 * @return {number}
 */
const lengthOfLongestSubstring = function(s) {
    let hashMap = {}
    let num = []

    for(let i = 0; i < s.length; i++) {
        i = hashMap[s[i]]
    }

    for(let i = 0; i < s.length; i++) {
        // if(s[i] )
    }

};